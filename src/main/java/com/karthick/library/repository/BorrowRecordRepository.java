package com.karthick.library.repository;

import com.karthick.library.model.Book;
import com.karthick.library.model.Student;

public class BorrowRecordRepository {

    public void addBorrowRecord(Book book,Student student) {
     //   String sql="INSERT INTO borrow_records (student_id, book_id, borrow_date) VALUES (?, ?, CURRENT_DATE)";
    }
}
